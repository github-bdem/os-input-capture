import OsInputCapture from '../os-input-capture.js';

let opts = {
    keyboardOptions: {},
    mouseOptions: {},
    windowOptions: {
        windowTitle: '*test-input.txt - Mousepad'
    }
};

let osInputCapture = new OsInputCapture(opts);
